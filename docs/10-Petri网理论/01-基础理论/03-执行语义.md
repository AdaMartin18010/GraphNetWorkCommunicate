# Petri网的执行语义 / Execution Semantics of Petri Nets

## 📚 **概述 / Overview**

本文档介绍Petri网的执行语义，包括标识、变迁的可触发性和触发规则。

---

## 📑 **目录 / Table of Contents**

- [Petri网的执行语义 / Execution Semantics of Petri Nets](#petri网的执行语义--execution-semantics-of-petri-nets)
  - [📚 **概述 / Overview**](#-概述--overview)
  - [📑 **目录 / Table of Contents**](#-目录--table-of-contents)
  - [1. Petri网的执行语义 / Execution Semantics](#1-petri网的执行语义--execution-semantics)
    - [1.1 标识 / Marking](#11-标识--marking)
    - [1.2 变迁的可触发性 / Enabling of Transitions](#12-变迁的可触发性--enabling-of-transitions)
    - [1.3 变迁的触发 / Firing of Transitions](#13-变迁的触发--firing-of-transitions)
    - [1.4 可达性 / Reachability](#14-可达性--reachability)

---

## 1. Petri网的执行语义 / Execution Semantics

### 1.1 标识 / Marking

**定义 1.3** (标识 / Marking)

Petri网 $N = (P, T, F, W, M_0)$ 的**标识**是一个函数：
$$M: P \to \mathbb{N}$$

表示每个库所中的令牌数量。

### 1.2 变迁的可触发性 / Enabling of Transitions

**定义 1.4** (变迁的可触发性 / Enabling of Transitions)

在标识 $M$ 下，变迁 $t \in T$ **可触发**（enabled），当且仅当：
$$\forall p \in \prescript{}{}{t}: M(p) \geq W(p, t)$$

即，变迁 $t$ 的所有输入库所中的令牌数都大于等于相应边的权重。

### 1.3 变迁的触发 / Firing of Transitions

**定义 1.5** (变迁的触发 / Firing of Transitions)

如果变迁 $t$ 在标识 $M$ 下可触发，那么它可以**触发**（fire），产生新标识 $M'$：
$$M'(p) = M(p) - W(p, t) + W(t, p)$$

其中：

- 如果 $p \in \prescript{}{}{t}$，则消耗 $W(p, t)$ 个令牌
- 如果 $p \in t^{\bullet}$，则产生 $W(t, p)$ 个令牌
- 否则保持不变

**形式化定义**：

$$
M'(p) = \begin{cases}
M(p) - W(p, t) + W(t, p) & \text{if } p \in \prescript{}{}{t} \cup t^{\bullet} \\
M(p) & \text{otherwise}
\end{cases}
$$

### 1.4 可达性 / Reachability

**定义 1.6** (可达性 / Reachability)

标识 $M'$ 从标识 $M$ **可达**（reachable），记作 $M \xrightarrow{*} M'$，如果存在一个变迁序列 $t_1, t_2, \ldots, t_k$，使得：
$$M \xrightarrow{t_1} M_1 \xrightarrow{t_2} M_2 \xrightarrow{t_3} \cdots \xrightarrow{t_k} M'$$

**可达集**（Reachability Set）定义为：
$$R(N, M_0) = \{M \mid M_0 \xrightarrow{*} M\}$$

---

**文档版本**: v1.0
**创建时间**: 2025年1月
**质量等级**: ⭐⭐⭐⭐⭐ 五星级
