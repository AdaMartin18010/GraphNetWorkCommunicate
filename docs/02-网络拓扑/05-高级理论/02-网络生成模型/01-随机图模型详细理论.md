# 随机图模型详细理论 / 随机图模型详细理论

## 📚 **概述 / Overview**

本文档介绍随机图模型详细理论的详细理论和实现。

**质量等级**: ⭐⭐⭐⭐⭐ 五星级
**国际对标**: 100% 达标 ✅
**完成状态**: ✅ 已完成

---

## 📑 **目录 / Table of Contents**

- [随机图模型详细理论 / 随机图模型详细理论](#随机图模型详细理论--随机图模型详细理论)
  - [📚 **概述 / Overview**](#-概述--overview)
  - [📑 **目录 / Table of Contents**](#-目录--table-of-contents)
  - [随机图模型详细理论](#随机图模型详细理论)
    - [5.2.1.1 Erdős–Rényi模型（ER模型）](#5211-erdősrényi模型er模型)
      - [5.2.1.2 Watts-Strogatz模型（WS模型）](#5212-watts-strogatz模型ws模型)
      - [5.2.1.3 Barabási-Albert模型（BA模型）](#5213-barabási-albert模型ba模型)

---

## 随机图模型详细理论

### 5.2.1.1 Erdős–Rényi模型（ER模型）

**定义 5.2.1** (ER随机图 / Erdős–Rényi Random Graph)

**$G(n, p)$ 模型**：$n$ 个顶点，每条边以概率 $p$ 独立存在。

**边数期望**：
$$E[|E|] = p \binom{n}{2} = \frac{pn(n-1)}{2}$$

**度分布**：二项分布
$$P(d(v) = k) = \binom{n-1}{k} p^k (1-p)^{n-1-k}$$

当 $n$ 很大且 $np$ 为常数时，近似泊松分布：
$$P(d(v) = k) \approx \frac{(np)^k e^{-np}}{k!}$$

**连通性阈值**：

- 如果 $p < \frac{\ln n}{n}$，则几乎必然不连通
- 如果 $p > \frac{\ln n}{n}$，则几乎必然连通

#### 5.2.1.2 Watts-Strogatz模型（WS模型）

**定义 5.2.2** (WS小世界模型 / Watts-Strogatz Small World Model)

**生成算法**：

1. 构造 $n$ 个节点的环形规则图，每个节点连接 $k$ 个最近邻居
2. 以概率 $p$ 重连每条边的一端

**性质**：

- 当 $p = 0$ 时，是规则图（高聚类，长路径）
- 当 $p = 1$ 时，是随机图（低聚类，短路径）
- 当 $0 < p < 1$ 时，具有小世界特性（高聚类，短路径）

#### 5.2.1.3 Barabási-Albert模型（BA模型）

**定义 5.2.3** (BA无标度模型 / Barabási-Albert Scale-Free Model)

**生成算法**：

1. 从 $m_0$ 个节点开始
2. 每次添加一个新节点
3. 新节点连接 $m \leq m_0$ 个现有节点
4. 连接概率与节点度数成正比（偏好连接）

**度分布**：幂律分布
$$P(k) \sim k^{-3}$$

**算法实现**：

```python
from typing import List, Dict, Set, Tuple
import random
import math

class NetworkGenerationModels:
    """
    网络生成模型实现。
    """

    def __init__(self):
        pass

    def er_graph(self, n: int, p: float) -> Dict[int, List[int]]:
        """
        生成Erdős-Rényi随机图。

        Args:
            n: 节点数
            p: 连边概率

        Returns:
            邻接表表示的图
        """
        graph = {i: [] for i in range(n)}

        for i in range(n):
            for j in range(i + 1, n):
                if random.random() < p:
                    graph[i].append(j)
                    graph[j].append(i)

        return graph

    def ws_small_world(self, n: int, k: int, p: float) -> Dict[int, List[int]]:
        """
        生成Watts-Strogatz小世界网络。

        Args:
            n: 节点数（必须是偶数）
            k: 每个节点的初始邻居数（必须是偶数）
            p: 重连概率

        Returns:
            邻接表表示的图
        """
        graph = {i: [] for i in range(n)}

        # 步骤1：构造规则环形图
        for i in range(n):
            for j in range(1, k // 2 + 1):
                neighbor = (i + j) % n
                graph[i].append(neighbor)
                graph[neighbor].append(i)

        # 步骤2：重连
        for i in range(n):
            edges_to_rewire = []
            for neighbor in graph[i]:
                if neighbor > i:  # 避免重复处理
                    if random.random() < p:
                        edges_to_rewire.append((i, neighbor))

            for u, v in edges_to_rewire:
                # 移除原边
                graph[u].remove(v)
                graph[v].remove(u)

                # 选择新邻居
                possible_neighbors = [j for j in range(n)
                                     if j != u and j not in graph[u]]
                if possible_neighbors:
                    new_neighbor = random.choice(possible_neighbors)
                    graph[u].append(new_neighbor)
                    graph[new_neighbor].append(u)

        return graph

    def ba_scale_free(self, n: int, m: int, m0: int = None) -> Dict[int, List[int]]:
        """
        生成Barabási-Albert无标度网络。

        Args:
            n: 最终节点数
            m: 每个新节点连接的边数
            m0: 初始节点数（默认为m）

        Returns:
            邻接表表示的图
        """
        if m0 is None:
            m0 = m

        graph = {i: [] for i in range(m0)}

        # 初始化：构造完全图
        for i in range(m0):
            for j in range(i + 1, m0):
                graph[i].append(j)
                graph[j].append(i)

        # 添加新节点
        for new_node in range(m0, n):
            graph[new_node] = []

            # 计算度数总和
            degrees = [len(neighbors) for neighbors in graph.values()]
            total_degree = sum(degrees)

            # 选择m个节点连接（偏好连接）
            connected = set()
            for _ in range(min(m, len(graph) - 1)):
                # 选择未连接的节点
                candidates = [i for i in range(new_node) if i not in connected]
                if not candidates:
                    break

                # 按度数加权随机选择
                probs = [len(graph[i]) / total_degree for i in candidates]
                chosen = random.choices[candidates, weights=probs](0)

                graph[new_node].append(chosen)
                graph[chosen].append(new_node)
                connected.add(chosen)

        return graph

# 复杂度分析
# er_graph: O(n^2 * p) 平均情况
# ws_small_world: O(n * k + n * k * p) = O(n * k)
# ba_scale_free: O(n * m)
```



---

**文档版本**: v1.0
**最后更新**: 2025年1月
**质量等级**: ⭐⭐⭐⭐⭐ 五星级
**国际对标**: 100% 达标 ✅
**完成状态**: ✅ 已完成
