# 算法选择因果链 / Algorithm Selection Causal Chain

## 📚 **概述 / Overview**

本文档提供算法选择的因果链分析，从目标需求到算法选择的推理路径。

---

## ⚡ **目标 → 算法族选择因果链 / Goal → Algorithm Family Selection Causal Chain**

```mermaid
graph TD
    Goal[分析目标] --> CheckType{目标类型?}
    CheckType -->|形式化验证| FormalAlg[形式化算法]
    CheckType -->|时序追踪| TemporalAlg[时序算法]
    CheckType -->|形状分析| TopologyAlg[拓扑算法]

    FormalAlg --> Reach[可达性算法]
    FormalAlg --> Invariant[不变量算法]
    FormalAlg --> ModelCheck[模型检验算法]

    TemporalAlg --> Path[路径算法]
    TemporalAlg --> Spectrum[谱算法]
    TemporalAlg --> Community[社区算法]

    TopologyAlg --> Homology[同调算法]
    TopologyAlg --> Persist[持续同调算法]
    TopologyAlg --> Hodge[Hodge算法]

    Reach --> ReachResult[可达性结果]
    Invariant --> InvResult[不变量结果]
    ModelCheck --> CheckResult[检验结果]

    Path --> PathResult[路径结果]
    Spectrum --> SpecResult[谱结果]
    Community --> CommResult[社区结果]

    Homology --> HomResult[同调结果]
    Persist --> PersistResult[持久结果]
    Hodge --> HodgeResult[Hodge结果]
```

---

## 📊 **规模 → 算法策略选择因果链 / Scale → Algorithm Strategy Selection Causal Chain**

```mermaid
graph LR
    Scale[系统规模] --> CheckSize{规模大小?}
    CheckSize -->|>1e5| Large[大规模策略]
    CheckSize -->|<1e5| Small[中小规模策略]

    Large --> Incremental[增量算法]
    Large --> Approximate[近似算法]
    Large --> Sampling[采样算法]

    Small --> Exact[精确算法]
    Small --> Full[完整算法]

    Incremental --> IncResult[增量结果]
    Approximate --> ApproxResult[近似结果]
    Sampling --> SampleResult[采样结果]

    Exact --> ExactResult[精确结果]
    Full --> FullResult[完整结果]
```

---

## ⏱️ **实时性 → 算法选择因果链 / Real-time → Algorithm Selection Causal Chain**

```mermaid
graph TD
    RealTime[实时性要求] --> CheckLatency{延迟要求?}
    CheckLatency -->|严格<100ms| Stream[流式算法]
    CheckLatency -->|宽松>1s| Batch[批处理算法]

    Stream --> StreamAlg[流式算法族]
    StreamAlg --> IncPageRank[增量PageRank]
    StreamAlg --> IncShortest[增量最短路]
    StreamAlg --> StreamDetect[流式检测]

    Batch --> BatchAlg[批处理算法族]
    BatchAlg --> FullReach[完整可达性]
    BatchAlg --> FullInv[完整不变量]
    BatchAlg --> FullTopology[完整拓扑分析]

    IncPageRank --> StreamResult[流式结果]
    IncShortest --> StreamResult
    StreamDetect --> StreamResult

    FullReach --> BatchResult[批处理结果]
    FullInv --> BatchResult
    FullTopology --> BatchResult
```

---

## 🎯 **精度要求 → 算法参数选择因果链 / Accuracy → Algorithm Parameter Selection Causal Chain**

```mermaid
graph LR
    Accuracy[精度要求] --> CheckPrecision{精度级别?}
    CheckPrecision -->|高精度| High[高精度算法]
    CheckPrecision -->|中等精度| Medium[中等精度算法]
    CheckPrecision -->|低精度| Low[低精度算法]

    High --> ExactAlg[精确算法]
    High --> FullSpace[完整状态空间]

    Medium --> ApproxAlg[近似算法]
    Medium --> PartialSpace[部分状态空间]

    Low --> SamplingAlg[采样算法]
    Low --> SketchAlg[Sketch算法]

    ExactAlg --> HighResult[高精度结果]
    FullSpace --> HighResult

    ApproxAlg --> MediumResult[中等精度结果]
    PartialSpace --> MediumResult

    SamplingAlg --> LowResult[低精度结果]
    SketchAlg --> LowResult
```

---

## 📋 **因果链说明 / Causal Chain Description**

### 算法选择决策路径

1. **目标识别**: 确定分析目标（形式化验证、时序追踪、形状分析）
2. **规模评估**: 评估系统规模（大规模、中小规模）
3. **实时性评估**: 评估实时性要求（严格、宽松）
4. **精度评估**: 评估精度要求（高、中、低）
5. **算法选择**: 根据以上因素选择算法族和策略
6. **参数设定**: 根据精度要求设定算法参数

### 典型决策路径

- **路径1**: 形式化验证 + 中小规模 + 宽松实时性 + 高精度 → 精确可达性算法 + 完整状态空间
- **路径2**: 时序追踪 + 大规模 + 严格实时性 + 中等精度 → 增量算法 + 流式处理
- **路径3**: 形状分析 + 大规模 + 宽松实时性 + 低精度 → 采样算法 + 近似拓扑分析

---

## 📚 **参考文档 / Reference Documents**

- [算法选择认知路径](../../决策逻辑图谱/02-算法选择认知路径.md)
- [模型选择认知路径](../../决策逻辑图谱/01-模型选择认知路径.md)
- [应用模式归纳概述](../../../13-应用模式归纳/00-应用模式归纳概述.md)

---

**文档版本**: v1.0
**创建时间**: 2025年1月
**最后更新**: 2025年1月
**维护者**: GraphNetWorkCommunicate项目组
**状态**: ✅ 完成
