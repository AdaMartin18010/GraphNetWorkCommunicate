# 概念关系网络图 / Concept Relationship Network Graph

## 📚 **概述 / Overview**

本文档提供三大理论核心概念的关系网络图，展示概念间的结构关系、动态关系和分析关系。

---

## 🔗 **Petri网概念关系网络 / Petri Net Concept Relationship Network**

```mermaid
graph TD
    PN[Petri网] --> P[库所Place]
    PN --> T[变迁Transition]
    PN --> F[流关系Flow]

    P --> Token[令牌Token]
    P --> Marking[标识Marking]

    T --> Firing[变迁点火]
    T --> PreSet[前集·t]
    T --> PostSet[后集t·]

    Marking --> M0[初始标识M₀]
    Marking --> Reach[可达标识]

    Firing --> Reachability[可达性]
    Firing --> StateEq[状态方程]

    Reachability --> ReachGraph[可达图]

    PN --> Invariant[不变量]
    Invariant --> SInv[S-不变量]
    Invariant --> TInv[T-不变量]

    SInv --> Conserve[资源守恒]
    TInv --> Cycle[循环行为]

    PN --> Property[性质]
    Property --> Liveness[活性]
    Property --> Bounded[有界性]
    Property --> Safety[安全性]

    Liveness --> Deadlock[死锁检测]
    Bounded --> CoverTree[覆盖树]
```

---

## 🔗 **动态图概念关系网络 / Dynamic Graph Concept Relationship Network**

```mermaid
graph TD
    DG[动态图论] --> TempNet[时序网络]
    DG --> EvolGraph[演化图]
    DG --> CausalGraph[因果图]

    TempNet --> Snapshot[快照序列]
    TempNet --> TempAdj[时序邻接矩阵]

    EvolGraph --> Rewrite[图重写]
    EvolGraph --> EdgeEvol[边演化]

    CausalGraph --> DoCalc[Do演算]
    CausalGraph --> SEM[结构方程模型]

    DG --> Path[路径存在性]
    DG --> Connect[连通性]
    DG --> Spectrum[谱演化]

    Path --> TempPath[时间尊重路径]
    Connect --> Community[社区发现]
    Spectrum --> Eigenval[特征值演化]

    DG --> Algorithm[算法]
    Algorithm --> Incremental[增量算法]
    Algorithm --> Embedding[图嵌入]
    Algorithm --> GNN[图神经网络]

    Incremental --> IncPageRank[增量PageRank]
    Incremental --> IncShortest[增量最短路]

    Embedding --> DeepWalk[DeepWalk]
    Embedding --> Node2Vec[Node2Vec]

    GNN --> GCN[图卷积网络]
    GNN --> GraphSAGE[GraphSAGE]
    GNN --> GAT[图注意力网络]
```

---

## 🔗 **拓扑模型概念关系网络 / Topology Model Concept Relationship Network**

```mermaid
graph TD
    TM[拓扑模型] --> Complex[胞腔复形]
    TM --> CW[CW复形]
    TM --> Simp[单纯复形]

    Complex --> Cell[胞腔]
    Cell --> Cell0[0-胞腔点]
    Cell --> Cell1[1-胞腔边]
    Cell --> Cell2[2-胞腔面]

    TM --> Chain[链复形]
    Chain --> Boundary[边缘算子∂]
    Chain --> ChainGroup[链群C_i]

    Boundary --> Homology[同调群H_i]
    Boundary --> Cohomology[上同调群H^i]

    Homology --> Betti[贝蒂数β_i]
    Cohomology --> Hodge[Hodge理论]

    TM --> Persist[持续同调]
    Persist --> Filtration[过滤]
    Persist --> PersistDiag[持久图]

    TM --> TDA[拓扑数据分析]
    TDA --> Mapper[Mapper算法]
    TDA --> Shape[形状分析]

    Hodge --> Harmonic[谐波]
    Hodge --> Gradient[梯度]
    Hodge --> Curl[旋度]
```

---

## 🔗 **跨理论概念映射关系 / Cross-Theory Concept Mapping**

```mermaid
graph LR
    subgraph PN[Petri网]
        P1[库所P]
        T1[变迁T]
        M1[标识M]
        F1[点火]
    end

    subgraph DG[动态图]
        V1[顶点V]
        E1[边E]
        G1[快照G_t]
        R1[图重写]
    end

    subgraph TM[拓扑模型]
        C1[胞腔C_i]
        D1[边缘算子∂]
        H1[同调群H_i]
        L1[同伦]
    end

    P1 -.映射.-> V1
    T1 -.映射.-> E1
    M1 -.映射.-> G1
    F1 -.映射.-> R1

    V1 -.映射.-> C1
    E1 -.映射.-> D1
    G1 -.映射.-> H1
    R1 -.映射.-> L1

    C1 -.映射.-> P1
    D1 -.映射.-> T1
    H1 -.映射.-> M1
    L1 -.映射.-> F1
```

---

## 📋 **关系类型说明 / Relationship Type Description**

### 结构关系

- **包含关系**: 理论 → 概念（如Petri网 → 库所）
- **组成关系**: 概念 → 子概念（如标识 → 令牌）
- **连接关系**: 概念 ↔ 概念（如库所 ↔ 变迁）

### 动态关系

- **因果关系**: 概念 → 结果（如变迁点火 → 可达性）
- **演化关系**: 概念 → 演化（如快照 → 时序网络）
- **转换关系**: 概念 ↔ 概念（如标识 ↔ 快照）

### 分析关系

- **计算关系**: 概念 → 方法（如可达性 → 可达图）
- **性质关系**: 概念 → 性质（如Petri网 → 活性）
- **映射关系**: 概念 ↔ 概念（如库所 ↔ 顶点）

---

## 📚 **参考文档 / Reference Documents**

- [View文件夹概念关系网络](../../../../view/View文件夹概念关系网络-2025.md)
- [理论映射关系图谱](../../00-理论映射关系图谱.md)
- [Petri网理论逻辑脉络](../../01-理论逻辑脉络/01-Petri网理论逻辑脉络.md)

---

**文档版本**: v1.0
**创建时间**: 2025年1月
**最后更新**: 2025年1月
**维护者**: GraphNetWorkCommunicate项目组
**状态**: ✅ 完成
