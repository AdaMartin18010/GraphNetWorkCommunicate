# 批量标记执行计划 / Batch Marking Execution Plan 2025

## 📋 执行概述

为153个待完善文件添加标记，完成项目文件梳理的最后一步。

## 📊 当前状态

- **待标记文件总数**: 153个
- **已标记文件数**: 1个（示例文件：动态图理论）
- **待标记文件数**: 152个

## 🎯 执行目标

- [ ] 为所有153个待完善文件添加标记
- [ ] 验证标记结果
- [ ] 更新相关文档

## 🚀 执行步骤

### 步骤1: 备份文件（推荐）

**Windows**:

```powershell
New-Item -ItemType Directory -Path "backup-pending-files" -Force
Get-ChildItem -Path "docs" -Recurse -Filter "*-深度改进版-2025.md" -File | Copy-Item -Destination "backup-pending-files\" -Recurse
```

**Linux/Mac**:

```bash
mkdir -p backup-pending-files
find docs -name "*-深度改进版-2025.md" -type f -exec cp --parents {} backup-pending-files/ \;
```

### 步骤2: 执行批量标记脚本

**Windows**:

```powershell
cd E:\_src\GraphNetWorkCommunicate
powershell -ExecutionPolicy Bypass -File scripts/mark-pending-files.ps1
```

**Linux/Mac**:

```bash
cd /path/to/GraphNetWorkCommunicate
bash scripts/mark-pending-files.sh
```

### 步骤3: 验证标记结果

**检查已标记文件数量**:

**Windows**:

```powershell
(Get-ChildItem -Path "docs" -Recurse -Filter "*-深度改进版-2025.md" -File | Select-String -Pattern "⚠️ \*\*状态\*\*: 待完善").Count
```

**Linux/Mac**:

```bash
grep -r "⚠️ \*\*状态\*\*: 待完善" docs/**/*-深度改进版-2025.md | wc -l
```

**预期结果**: 153个文件

### 步骤4: 检查标记格式

随机检查几个文件，确认标记格式正确：

**Windows**:

```powershell
Get-Content docs\01-图论基础\05-高级理论-图同构理论-深度改进版-2025.md | Select-Object -First 15
```

**Linux/Mac**:

```bash
head -15 docs/01-图论基础/05-高级理论-图同构理论-深度改进版-2025.md
```

## ✅ 验证清单

执行后检查：

- [ ] 已标记文件数量 = 153个
- [ ] 标记格式正确（包含⚠️和📝图标）
- [ ] 文件内容未被破坏
- [ ] 所有待完善文件都已标记
- [ ] 已标记文件可以正常打开和阅读

## 📝 执行后更新

标记完成后，更新以下文档：

1. **更新待完善文件清单**
   - 标记状态：已标记
   - 标记时间：2025年1月

2. **更新工作状态总览**
   - 工作完成度：100%
   - 标记状态：已完成

3. **更新快速参考**
   - 标记状态：已完成

## 🔧 故障排除

### 如果脚本执行失败

1. **检查文件权限**
   - 确保有文件写入权限

2. **检查文件编码**
   - 确保文件是UTF-8编码

3. **手动标记示例**
   - 可以先手动标记一个文件作为测试
   - 确认标记格式正确后再批量执行

### 如果标记格式不正确

1. **检查脚本中的标记内容**
   - 确认标记文本正确
   - 确认换行符正确

2. **手动修复**
   - 如果只有少数文件格式不正确，可以手动修复

## 📚 相关文档

- [执行指南](./scripts/执行指南.md) - 详细执行步骤
- [待完善文件标记说明](./待完善文件标记说明-2025.md) - 标记说明
- [待完善文件处理优先级清单](./待完善文件处理优先级清单-2025.md) - 优先级分类

---

**计划创建时间**: 2025年1月
**执行状态**: ⏳ 待执行
**维护者**: GraphNetWorkCommunicate项目组
